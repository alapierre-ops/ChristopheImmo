---
---

<section class="visits-section">
	<div class="visits-content">
		<div class="visits-header">
			<svg class="visits-icon" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
				<rect x="3" y="4" width="18" height="18" rx="2" ry="2"/>
				<line x1="16" y1="2" x2="16" y2="6"/>
				<line x1="8" y1="2" x2="8" y2="6"/>
				<line x1="3" y1="10" x2="21" y2="10"/>
			</svg>
			<h3>Gestion des visites</h3>
		</div>
		<div class="visits-text">
			<div class="visit-item">
				<svg class="visit-bullet" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
					<circle cx="12" cy="12" r="10"/>
				</svg>
				<p>
					Un rendez vous sera organisé rapidement après description détaillée de votre profil (situation précise, niveau d'études ou projet artistique, durée de location envisagée).
				</p>
			</div>
			<div class="visit-item">
				<svg class="visit-bullet" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
					<circle cx="12" cy="12" r="10"/>
				</svg>
				<p>
					Une visite en visioconférence pourra également être proposée en cas d'éloignement géographique justifié.
				</p>
			</div>
		</div>
	</div>
</section>

<style>
	.visits-section {
		margin: 3rem 0;
		padding: 0;
		background: linear-gradient(135deg, #e0c3fc 0%, #8ec5fc 100%);
		border-radius: 16px;
		overflow: hidden;
		box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
	}

	.visits-content {
		max-width: 900px;
		margin: 0 auto;
		padding: 2.5rem;
		background: white;
		margin: 2rem;
		border-radius: 12px;
	}

	.visits-header {
		display: flex;
		align-items: center;
		gap: 0.75rem;
		margin-bottom: 2rem;
		padding-bottom: 1rem;
		border-bottom: 2px solid #e0e7ff;
	}

	.visits-icon {
		color: #8b5cf6;
		flex-shrink: 0;
	}

	.visits-content h3 {
		font-size: 1.5rem;
		margin: 0;
		color: #1a1a1a;
		font-weight: 600;
	}

	.visits-text {
		display: flex;
		flex-direction: column;
		gap: 1.25rem;
	}

	.visit-item {
		display: flex;
		align-items: flex-start;
		gap: 1rem;
		padding: 1rem;
		background: #faf5ff;
		border-radius: 8px;
		border-left: 3px solid #8b5cf6;
		transition: all 0.2s ease;
	}

	.visit-item:hover {
		background: #f3e8ff;
		transform: translateX(4px);
	}

	.visit-bullet {
		color: #8b5cf6;
		flex-shrink: 0;
		margin-top: 0.25rem;
	}

	.visit-item p {
		font-size: 1.05rem;
		line-height: 1.7;
		margin: 0;
		color: #4a5568;
		flex: 1;
	}

	@media (max-width: 768px) {
		.visits-content {
			padding: 1.5rem;
			margin: 1rem;
		}

		.visits-header {
			margin-bottom: 1.5rem;
		}

		.visits-content h3 {
			font-size: 1.25rem;
		}

		.visit-item {
			padding: 0.875rem;
		}

		.visit-item p {
			font-size: 0.95rem;
		}

		.visit-bullet {
			width: 14px;
			height: 14px;
		}
	}
</style>

